# STEP 5 — me v4 포트 실행 스펙 v2 (정확히 v0 디자인 + 실배선 + §0 + 매장관리 무접촉)

대상: `/me` (`src/routes/_user/me.tsx`)
디자인 소스: `docs/ref/me.tsx` (v0 me v4)
목표: **어질러진 me를 v0 디자인으로 정확히 정돈 + 실데이터/실기능 배선.** 결제·§0·매장관리 보존.
전제: 라이브 me는 이미 상당부분 v4. → **남은 v0 비주얼을 정확히 반영 + 실배선 확정.**

---

## 핵심 원칙 — "실배선"이 §0을 자동으로 지킨다
- v0 mock의 **가짜 숫자(드로피 1240·30 dropy)는 실배선이 아니다.** 실 드로피 데이터는 §0상 **"준비중"**.
- 즉 **v0 비주얼/레이아웃 + 실데이터** = 드로피는 자동으로 "준비중". 가짜 mock 숫자 금지.
- v0 "사용" 버튼(핸들러 없는 mock)이 아니라 **라이브 실사용 플로우**를 배선.

## 🔴 절대 보존 (무접촉/무재구현)
1. **결제** — 캐시 탭 = 라이브 `<CashSection/>` 그대로. useHectoCharge·약관3·이용내역·[결제취소]·Hecto API/server/DB **무접촉**.
2. **§0 드로피** — 드로피 탭·히어로 자산요약의 드로피 = **"준비중"**(숫자·원화 표시 금지). v0 드로피 잔액/멤버십 mock **절대 적용 금지**.
3. **★ 매장관리** — **디자인 수정 없음. 기능 전부 통째로 유지.** 내 매장 → `/partner`(또는 현행 매장관리 섹션) **그대로**. v0 리스타일 적용하지 마라.

## v0 디자인 정확히 반영 (실배선 + 기능 유지)
- **헤더** → v0: MeMark(로고) + "내 페이지" + 🔔(→`/inbox`) + ⚙️(설정). ("나" 텍스트 대체)
- **설정** → v0: 헤더 기어 → **인라인 아코디언**(#418, 바텀시트/포털 아님) + 로그아웃(기존 signOut/AlertDialog **보존**).
- **프로필 히어로** → v0 잉크 카드 비주얼 + 라이브(아바타·이름·비즈배지·이메일·편집→`/profile`).
- **자산요약 스트립** → v0 비주얼 + 라이브. **드로피 = "준비중"**(§0). 캐시 잔액은 CashSection 소관.
- **지갑 3탭**(캐시/쿠폰/드로피) → v0 세그먼트 비주얼 + 기존 walletTab:
  - 캐시 = `<CashSection/>` 그대로(결제).
  - 쿠폰 = **v0 티켓 카드 비주얼** + **라이브 CouponClaimCard 실사용 플로우 배선**(/coupon/$claim_code·발신자·재공유). mock "사용" 버튼 금지.
  - 드로피 = **v0 탭 레이아웃 + "준비중"**(§0).
- **만든 카드** → **라이브 리스트 유지**(/results·/card-edit·임베드) + v0 톤 정돈. (v0의 "버튼 1개 축소" 적용 금지 — 기능 유지)
- **받은 쿠폰 메이커** → **유지**(대표카드 임베드+구독) + v0 톤.
- **구독한 매장 / 내 주문(→/me-orders)** → v0 비주얼 + 라이브.

## 무접촉
Hecto API·server·DB·RPC · CashSection/useHectoCharge 로직 · **매장관리(/partner) 디자인·기능** · `feed-queries` · `CardBody.*` · `adapters` · `share-journey` · `studio-build` · `explore` · 홈 컴포넌트.
mock 금지(라이브 로더만). `git add -A`/`stash`/`commit`/`wrangler deploy` 금지.

## 디자인 락
무채색 + **#2563EB** · 이모지 금지 · **blink/pulse 금지** · 60대 · **Radix Dialog/Sheet/Drawer 금지**(인라인).

## 검증
1. `bun run build` 성공.
2. ★ **결제 무결성**: 캐시 탭 = CashSection 그대로 · 약관 게이트 · [결제취소] · 잔액 폴링 동일.
3. ★ **§0**: 드로피 = "준비중"(숫자 0). v0 드로피 mock 미유입.
4. ★ **매장관리**: 디자인·기능 그대로(무접촉 확인).
5. 쿠폰 = 실사용 플로우 동작 · 만든카드 리스트·받은쿠폰메이커 유지.
6. 헤더/설정/자산요약/지갑탭 = v0 비주얼.
7. mock 데이터 0(라이브만). 무접촉 파일 손 안 댐. 커밋·배포 안 함.
